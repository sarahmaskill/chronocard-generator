const Lieutenant = require ('./Lieutenant');
const Deputy = require ('./Deputy');
const Enforcer = require('./Enforcer');
const Shows = require('./Shows')

module.exports = { Lieutenant, Deputy, Enforcer}